# Exact versions available in local PyPI repository
# These match the packages we uploaded to bookverse-pypi-cache-local
fastapi==0.111.0
uvicorn==0.30.0
requests==2.31.0
pytest==8.3.2
pytest-cov==4.0.0
httpx==0.27.0
