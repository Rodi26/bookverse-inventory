# Minimal requirements avoiding complex dependency constraints
# Using only core packages without optional performance dependencies
fastapi==0.100.0
uvicorn[standard]==0.30.0
requests==2.31.0
pytest==8.3.2
pytest-cov==4.0.0
httpx==0.27.0
python-dotenv==1.1.1
python-jose[cryptography]>=3.2.0
python-multipart==0.0.20

# Core dependencies that FastAPI needs (let FastAPI manage starlette version)
pydantic==2.11.9
typing-extensions==4.15.0
